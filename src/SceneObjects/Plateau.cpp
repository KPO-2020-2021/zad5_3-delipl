#include "Plateau.hpp"

// Plateau::Plateau(const Vector3 &position, const double &width, const double &length) : Cuboid(Vector3({width, length, width}) / 10, position)
// {
//     // this->UpdatePoints();
// }